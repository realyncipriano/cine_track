org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
# org.gradle.java.home is intentionally omitted — JAVA_HOME env var is used instead
# (set to Android Studio JBR to avoid STATUS_ACCESS_VIOLATION on JDK version probe)
android.useAndroidX=true
android.enableJetifier=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
android.ndkVersion=27.0.12077973
android.cmakeVersion=3.31.6
